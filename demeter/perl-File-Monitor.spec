Name:           perl-File-Monitor
Version:        1.00
Release:        2%{?dist}
Summary:        Monitor files and directories for changes
License:        GPL+ or Artistic
Group:          Development/Libraries
URL:            http://search.cpan.org/dist/File-Monitor/
Source0:        http://www.cpan.org/authors/id/A/AN/ANDYA/File-Monitor-%{version}.tar.gz
BuildRoot:      %{_tmppath}/%{name}-%{version}-%{release}-root-%(%{__id_u} -n)
BuildArch:      noarch
BuildRequires:  perl-generators
BuildRequires:  perl(Module::Build)
BuildRequires:  perl(Test::More)
BuildRequires:  perl(version)
Requires:       perl(Test::More)
Requires:       perl(version)
Requires:       perl(:MODULE_COMPAT_%(eval "`%{__perl} -V:version`"; echo $version))
Provides:       perl(File::Monitor) = %{version}

%description
This module provides a simple interface for monitoring one or more files or
directories and reporting any changes that are made to them.

%prep
%setup -q -n File-Monitor-%{version}

%build
%{__perl} Build.PL --installdirs=vendor
./Build

%install
rm -rf $RPM_BUILD_ROOT

./Build install --destdir=$RPM_BUILD_ROOT --create_packlist=0
find $RPM_BUILD_ROOT -depth -type d -exec rmdir {} 2>/dev/null \;

%{_fixperms} $RPM_BUILD_ROOT/*

%check
./Build test

%files
%defattr(-,root,root,-)
%doc Changes examples README TODO
%{perl_vendorlib}/*
%{_mandir}/man3/*

%changelog
* Thu Jun 16 2022 Stuart Campbell (scampbell@bnl.gov) 1.00-2
- Added perl-generators to build dependencies

* Sun Apr 11 2021 Stuart Campbell (scampbell@bnl.gov) 1.00-1
- Specfile autogenerated by cpanspec 1.78.
